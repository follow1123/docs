---
title: init
sidebar:
    order: 0
---

## 初始化本地仓库

```bash
# 创建仓库的文件夹，并进入
mkdir example-repo
cd example-repo

# 初始化仓库
git init
```

## 初始化裸仓库

用于作为服务器端仓库

```bash
# 创建仓库的文件夹，并进入
mkdir example-bare
cd example-bare

# 初始化裸仓库
git init --bare
```
